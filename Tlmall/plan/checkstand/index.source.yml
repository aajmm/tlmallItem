getTemplate_uiControl29_mowSrv: "import {Component,Fragment} from 'react'; \nimport {CustomHeader} from 'ysp-custom-components';\nexport default class extends Component{\n//返回\n  back=()=>{ \n   let handler = this.props.customHandler;\n   if (handler) {\n    handler({\n    eventType: 'back'\n    });\n   }\n  }\n  //回到首页\n  home=()=>{ \n   let handler = this.props.customHandler;\n   if (handler) {\n    handler({\n    eventType: 'home'\n    });\n   }\n  }\n  render(){\n    let data = this.props.customData;\n    let me=this;\n    return(\n    \t<CustomHeader \n             data={{centerText:\"收银台\"}} \n             backIsShow={true} \n             homeIsShow={true}\n             back={me.back.bind(me)}\n             home={me.home.bind(me)}\n          />\n    )\n  }\n}"
getData_control45_uteOrC: |-
  if(!elem){
    return;
  }
doAction_uiControl29_mowSrv: |-
  let type = data.eventType;
  if(type == "back"){
     ysp.appMain.back();
  }else if(type == "home"){
     elem.querySelector(".tl-main-menu").querySelector(".logo").click();
  }
getTemplate_uiControl31_We1hNF: "import {Component,Fragment} from 'react'; \nexport default class extends Component{\n  handlerClick = (e) => {\n   let handler=this.props.customHandler;\n    let target=e.target;\n    if(handler){\n      handler({\n        data:target.selectedIndex,\n        eventType:\"click\"\n      })\n    }\n  }\n  render(){\n    let data = this.props.customData;\n    let me=this;\n    return(\n    \t<div className=\"ysp-checkstand\">123456</div>\n    )\n  }\n}"
getData_control46_UJkyMD: >-
  if(!elem){
    return;
  }

  let data={
    orderCount:"",
    orderId:"",
    branck:"",
    remark:"",
    payNum:"",
    tip:[],
    total:""
  };

  data.orderCount = elem.querySelector("#orderCount").textContent;

  data.orderId =
  elem.querySelector("#orderList").querySelectorAll(".one")[0].querySelector("p").textContent;

  data.branck =
  elem.querySelector("#orderList").querySelectorAll(".one")[1].querySelector("p").textContent;

  data.remark =
  elem.querySelector("#orderList").querySelectorAll(".one")[2].querySelector("p").textContent;

  data.payNum = elem.querySelector(".pay_money").querySelector("b").textContent;

  data.tip[0] =
  elem.querySelector("#pay_infos_hint").querySelector(".a").textContent;

  data.tip[1] =
  elem.querySelector("#pay_infos_hint").querySelector(".b").textContent;

  data.total = elem.querySelector("#paymentAmount").textContent;

  return data;
doAction_uiControl31_We1hNF: ''
